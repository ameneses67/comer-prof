---
import ContactForm from "./ContactForm.astro";
---

<section id="contacto" class="py-16 lg:py-32">
  <div class="container mx-auto px-4 text-center">
    <h2 class="text-4xl font-bold mb-12">Contacto</h2>
    <!-- <p class="mb-8 max-w-lg mx-auto text-lg">
      Ponte en contacto con nosotros y obtén una cotización sin compromiso
      alguno
    </p>
    <form
      name="contact"
      action="notificacion"
      method="POST"
      data-netlify="true"
      netlify-honeypot="bot-field"
      class="border-2 rounded-xl p-4 sm:p-8 max-w-md mx-auto bg-neutral-50 flex flex-col"
    >
      <p class="hidden">
        <input name="bot-field" />
      </p>
      <div class="flex flex-col sm:flex-row sm:gap-4">
        <input
          type="text"
          name="name"
          placeholder="Nombre"
          class="form-input w-full rounded-md bg-slate-50 border-gray-200 mb-4 text-stone-600 placeholder-stone-400"
          required
        />
        <input
          type="text"
          name="lastname"
          placeholder="Apellido"
          class="form-input w-full rounded-md bg-slate-50 border-gray-200 mb-4 text-stone-600 placeholder-stone-400"
        />
      </div>
      <p>
        <input
          type="tel"
          name="phone"
          placeholder="Teléfono"
          class="form-input w-full rounded-md bg-slate-50 border-gray-200 mb-4 text-stone-600 placeholder-stone-400"
          required
        />
      </p>
      <p>
        <input
          type="email"
          name="email"
          placeholder="Correo electrónico"
          class="form-input w-full rounded-md bg-slate-50 border-gray-200 mb-4 text-stone-600 placeholder-stone-400"
          required
        />
      </p>
      <p>
        <textarea
          name="message"
          id="message"
          cols="30"
          rows="10"
          placeholder="Mensaje"
          class="form-textarea w-full rounded-md bg-slate-50 border-gray-200 mb-4 text-stone-600 placeholder-stone-400"
          required></textarea>
      </p>
      <button
        type="submit"
        class="px-8 py-2 bg-cyan-400 hover:bg-sky-800 text-white text-lg font-bold rounded-md w-2/3"
        >Enviar</button
      >
    </form> -->
    <div
      id="cbox-eUa2IHzihwzRB9N0"
      class="border-2 rounded-xl mx-auto max-w-md overflow-hidden"
    >
    </div>
    <ContactForm />
  </div>
</section>
